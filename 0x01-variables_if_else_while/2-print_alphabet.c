#include <stdio.h>
/**
 * main - Entry
 * Description: 'the programs description'
 * Return: Always zero
 */
int main(void)
{
	char lc;

	for (lc = 'a'; lc <= 'z'; lc++)
	{
		putchar(lc);
	}
	putchar('\n');
	return (0);
}
