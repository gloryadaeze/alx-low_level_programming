#include <stdio.h>
/**
 * main- enter the phrase
 * Description: 'the programs description'
 * Return: zero if exited, otherwise non-zero
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
